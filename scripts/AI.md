# AI Instructions for scripts/

## Performance/Минимализм Баланс (СТРОГИЕ ПРАВИЛА)

### Приоритет: Производительность → Минимализм → Читаемость

**НЕЛЬЗЯ:**

- ❌ Файлы больше 200 строк (если > 200 строк — рефактори и разбивай)
- ❌ Дублировать код (2+ повторения = вынести в функцию)
- ❌ Дублировать константы (определяй один раз в начале или в config.*)
- ❌ Async/await где не нужно (если можно sync — делай sync)
- ❌ Лишние зависимости (каждый import = вес)
- ❌ Объекты/массивы в циклах (создавай вне цикла, переиспользуй)
- ❌ try/catch в горячих путях (если можно проверить заранее)
- ❌ Комментарии (вообще не пиши их)

**МОЖНО:**

- ✅ Переменные из одной буквы в циклах (`for (const p of projects)`)
- ✅ Императивный стиль вместо функционального (если быстрее)
- ✅ Мутации вместо immutability (если контролируется)
- ✅ Early returns вместо вложенных if
- ✅ Inline функции если вызываются 1 раз

**ОБЯЗАТЕЛЬНО:**

- 🔥 Выносить повторяющийся функционал в `common/utils.ts` или начало файла
- 🔥 Выносить константы в `config.*.ts` или начало файла
- 🔥 Проверять перед добавлением: "Это уже есть где-то?"
- 🔥 Бенчмаркить если меняешь критичный код (используй `common/bench.ts`)

### Правило 100-200 строк

Если файл > 200 строк:

1. Найди логические блоки (функции/секции)
2. Вынеси в отдельные файлы
3. Оставь в главном файле только координацию

### Правило DRY (Don't Repeat Yourself)

Если видишь повтор:

1. **2 раза** = подумай о выносе
2. **3+ раза** = ОБЯЗАТЕЛЬНО вынеси

Куда выносить:

- Общие утилиты → `common/utils.ts`
- Константы → `config.*.ts` или начало файла
- Специфичные функции → начало текущего файла

### Производительность > Всё

- Не пиши "красивый" код если он медленнее
- Используй циклы вместо `.map().filter().reduce()` если данных много
- Кэшируй результаты дорогих операций
- Избегай RegExp в циклах (компилируй заранее)

## Task Runner Architecture (НОВОЕ)

### Архитектура запуска задач:

- Основной интерактивный выбор проектов и задач: `scripts/index.ts`
- Файл для быстрого запуска задач: `task.ts` в корне проекта
- Команда запуска: `bun task [taskname|pipename] [package|alias]`
- Каждый проект выполняется в отдельном процессе для изоляции

### Логирование:

- Вне задач: используем `Project.log` (например, `project.log('build')`)
- Внутри задач: используем `Project.createLogger(taskname|contextname)` (например, `project.createLogger('compile')`)
- Это позволяет логировать в контексте конкретной задачи

### Параллельное выполнение:

- Каждый проект запускается в отдельном процессе через `bun run`
- Это обеспечивает изоляцию и позволяет отдельным проектам падать без остановки других
- Управление процессами осуществляется через `task.ts`
