# @alaq/quark

**Высокопроизводительный Реактивный Примитив.**

[![Read in English](https://img.shields.io/badge/Language-English-blue)](./README.md)

Quark — фундаментальный строительный блок экосистемы Alaq. Предоставляет абстракцию реактивного состояния с минимальными накладными расходами, оптимизированную для движков V8 (Chrome/Node.js/Bun). Спроектирован для обеспечения максимальной производительности, используя оптимизации скрытых классов (hidden classes) и прямой доступ к памяти.

### Ключевые Характеристики
*   **V8 Optimized:** Использование мономорфного кода и стабильных скрытых классов для максимальной скорости выполнения.
*   **Memory Efficient:** Минимальное потребление памяти благодаря отсутствию замыканий на каждый экземпляр.
*   **Decoupled:** Встроенная "Quantum Bus" (Квантовая Шина) обеспечивает событийно-ориентированную архитектуру без жесткой связности компонентов.

---

### Пример Использования

```typescript
import { Qu, quantumBus, CHANGE } from '@alaq/quark'

// 1. Инициализация (Частица)
// Создаем кварк в изолированном мире 'hardware'
// Дедупликация включена по умолчанию.
const cpuTemp = Qu({ 
  id: 'cpu_0', 
  value: 45, 
  realm: 'hardware',
  emitChanges: true 
})

// 2. Логика (Трансформация)
// Округляем значения перед сохранением
cpuTemp.pipe(t => Math.round(t))

// 3. Прямое Наблюдение (Локальная Реактивность)
cpuTemp.up(t => {
  if (t > 90) console.warn(`[Alert] CPU Overheat: ${t}°C`)
})

// 4. Системная Интеграция (Квантовая Шина)
// Модуль аналитики слушает весь мир 'hardware', не имея прямой ссылки на переменную
quantumBus.getRealm('hardware').on(CHANGE, ({ id, value }) => {
  Analytics.recordMetric(id, value)
})

// Обновление Состояния
cpuTemp(45.4) // Значение становится 45. Игнорируется, если уже было 45 (dedup).
cpuTemp(95)   // Значение становится 95. Срабатывает Alert и Analytics.
```

---

### 📚 Документация

*   **[Концепция](./docs/ru/CONCEPT.md)** — Философия архитектуры: Состояние как частицы, Квантовая Шина и Миры (Realms).
*   **[API Кварка](./docs/ru/API.md)** — Справочник по фабрике `Qu`, методам экземпляра и опциям.
*   **[API Шины](./docs/ru/BUS_API.md)** — Справочник по маршрутизации событий и межмодульному взаимодействию.