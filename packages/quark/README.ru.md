# @alaq/quark

**Высокопроизводительный Реактивный Примитив.**

[![Read in English](https://img.shields.io/badge/Language-English-blue)](./README.md)

Quark — это фундаментальный строительный блок экосистемы Alaq. Он предоставляет абстракцию реактивного состояния с нулевой стоимостью, оптимизированную специально для современных движков V8 (Chrome/Node.js/Bun).

### 📦 Установка

```bash
bun add @alaq/quark
# или
npm install @alaq/quark
```

---

### ⚡ Бенчмарки Производительности

Quark создан для высоконагруженных сценариев (игры, финансовые тикеры, телеметрия), где традиционные библиотеки вызывают паузы Garbage Collection.

| Метрика | @alaq/quark | Signals (Solid/Preact) | Observables (RxJS) |
| :--- | :--- | :--- | :--- |
| **Скорость Обновления** | **~10ns / op** | ~150ns / op | ~1000ns+ / op |
| **Скорость Создания** | **~0.15µs** | ~0.50µs | ~2.00µs |
| **Память на Юнит** | **~40 bytes** | ~80-120 bytes | ~200+ bytes |

*> Бенчмарки проведены на Bun v1.3 / V8. Замер скорости на 1 млн итераций.*

---

### 🛠 Пример Архитектуры

В этом сценарии мы разделяем **Источник Данных** (Engine) и **Интерфейс** (UI) с помощью Realms (Миров). Это позволяет UI быть полностью развязанным от сложной логики движка.

```typescript
import { Qu, quantumBus, CHANGE } from '@alaq/quark'

// --- Realm 1: "Engine" (Источник высокочастотных данных) ---
// Эти кварки представляют сырые данные сенсоров.
// Они транслируют изменения в шину 'engine'.
const rpm = Qu({ realm: 'engine', id: 'rpm', value: 0, emitChanges: true })
const temp = Qu({ realm: 'engine', id: 'temp', value: 60, emitChanges: true })
const fuel = Qu({ realm: 'engine', id: 'fuel', value: 100 })

// --- Realm 2: "UI" (Состояние Интерфейса) ---
// Этот кварк управляет визуальным элементом. Он живет в другом изолированном мире.
const alertVisible = Qu({ realm: 'ui', id: 'alert', value: false })

// --- Системная Логика ( "Проводка" ) ---
// Логика связи живет в шине, развязывая два мира.
// UI не обязан импортировать переменную 'rpm' напрямую.

const engineBus = quantumBus.getRealm('engine')

engineBus.on(CHANGE, ({ id, value }) => {
  // Логика: Если RPM > 8000, показать алерт в другом мире
  if (id === 'rpm') {
    if (value > 8000 && !alertVisible.value) {
      console.log('[System] RPM Critical! Showing Alert.')
      alertVisible(true) // Cross-realm обновление
    } else if (value <= 8000 && alertVisible.value) {
      alertVisible(false)
    }
  }
})

// --- Симуляция ---

// 1. Штатная работа
rpm(4000) 
// -> шина 'engine' излучает CHANGE { id: 'rpm', value: 4000 }
// -> Логика проверяет условие (False)
// -> мир 'ui' остается спокойным

// 2. Критическая нагрузка
rpm(8500)
// -> шина 'engine' излучает CHANGE { id: 'rpm', value: 8500 }
// -> Логика проверяет условие (True)
// -> alertVisible обновляется в true
// -> мир 'ui' излучает свои события
```

### Базовое Использование (Минимальный паттерн)

Если вам не нужны сложные Миры и требуется просто реактивная переменная, используйте `Qv` (сокращение для `Qu` с начальным значением).

```typescript
import { Qv } from '@alaq/quark'

// Создание и мгновенная установка значения
const count = Qv(10)

// Прямая подписка
count.up(val => console.log('Значение:', val))

// Обновление
count(11) // выведет: Значение: 11
```

---

### 📚 Документация

*   **[Концепция](./docs/ru/CONCEPT.md)** — Философия архитектуры: Состояние как частицы, Квантовая Шина и Миры (Realms).
*   **[Сравнение](./docs/ru/COMPARISON.md)** — Детальный разбор отличий от Signals и RxJS.
*   **[API Кварка](./docs/ru/API.md)** — Справочник по фабрике `Qu`, методам экземпляра и опциям.
*   **[API Шины](./docs/ru/BUS_API.md)** — Справочник по маршрутизации событий и межмодульному взаимодействию.