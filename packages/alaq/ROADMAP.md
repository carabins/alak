# ALAQ - Roadmap

> **ALAQ** - новое поколение state management библиотеки
> Построено поверх **ALAK** (packages/alak) с фокусом на простоту и элегантность

## Философия

**ALAQ** = **AL**AK **Q**uery interface

- **Простота** - минимум концепций, максимум выразительности
- **Поэтичность** - естественный язык вместо технического жаргона
- **Типобезопасность** - полная поддержка TypeScript
- **Гибкость** - система плагинов для адаптации под любой стиль

## Ключевые концепции

### 1. Realm (Пространство)
Namespace для организации состояния. Поддерживает иерархию (как Java packages).

```typescript
const app = realm('com.mycompany.app')
const admin = realm('com.mycompany.admin')
```

### 2. Origin (Источник)
Модель состояния - может быть классом или объектом.

```typescript
// Класс
class Counter extends UnionModel<'app'> {
  static modelName = 'counter'
  count = 0
  increment() { this.count++ }
}

// Объект
app.origin('settings', {
  theme: 'dark',
  language: 'en'
})
```

### 3. Q (Точка обращения)
Единый интерфейс для работы с состоянием через строковые пути.

```typescript
Q.ask('counter.count')              // получить
Q.tell('counter.count', 10)         // установить
Q.hear('counter.count', fn)         // подписаться
Q.call('counter.increment')         // вызвать метод
```

### 4. Style Plugins (Плагины стилей)
Расширяемая система для адаптации API под разные стили кодирования.

```typescript
import { natural, technical, poetic } from 'alaq/styles'

Q.use(natural)    // ask, tell, hear, call
Q.use(technical)  // get, set, watch, invoke
Q.use(poetic)     // seek, grant, witness, summon
```

## Текущий статус

### ✅ Концепция определена
- [x] Философия и именование
- [x] Базовая архитектура (realm/origin/Q)
- [x] Система плагинов стилей
- [x] TypeScript типизация

### 🚧 В разработке
- [ ] Детализация TypeScript типов
- [ ] API для кросс-realm listeners
- [ ] Поддержка factory моделей (множественные экземпляры)
- [ ] Интеграция с ALAK

### 📋 Планируется
- [ ] Реализация ядра
- [ ] Встроенные стили (natural, technical, poetic)
- [ ] Примеры и тесты
- [ ] Документация
- [ ] Миграция с ALAK

## Сравнение с ALAK

| Аспект | ALAK (v4) | ALAQ (v5) |
|--------|-----------|-----------|
| **Инициализация** | `GetUnionCore('name')` | `realm('com.company.app')` |
| **Регистрация** | `uc.addAtom(config)` | `app.origin('counter', Counter)` |
| **Чтение** | `uc.facade.counterCore.count.value` | `Q.ask('counter.count')` |
| **Запись** | `uc.facade.counterCore.count(10)` | `Q.tell('counter.count', 10)` |
| **Подписка** | `uc.facade.counterCore.count.up(fn)` | `Q.hear('counter.count', fn)` |
| **Вызов** | `uc.facade.actions.counter.increment()` | `Q.call('counter.increment')` |
| **Типизация** | Частичная | Полная через строковые пути |
| **Расширяемость** | Жесткая | Гибкая через плагины |

## Обратная совместимость

ALAQ строится поверх ALAK, не заменяя его:
- ALAK остается в `packages/alak`
- ALAQ в новом `packages/alaq`
- Можно использовать оба одновременно
- `realm()` внутри создает `UnionCore`
- Доступ к старому API через `realm.facade`

## Roadmap по фазам

### Phase 1: Core (Текущая)
- Определение API
- Архитектура
- Базовые типы TypeScript

### Phase 2: Implementation
- Реализация realm/origin
- Реализация Q
- Система плагинов

### Phase 3: Styles
- Встроенные стили
- Документация для создания стилей
- Community styles

### Phase 4: Integration
- Интеграция с Vue (@alaq/vue)
- Интеграция с React
- DevTools

### Phase 5: Polish
- Оптимизация
- Документация
- Примеры
- Миграционные гайды

## Принципы разработки

1. **Простота превыше всего** - API должен быть интуитивным
2. **TypeScript first** - полная типизация из коробки
3. **Обратная совместимость** - не ломаем ALAK
4. **Поэтичность** - код должен быть красивым
5. **Расширяемость** - плагины для любого стиля

## Вопросы для обсуждения

- [ ] Naming: ask/tell/hear/call vs другие варианты?
- [ ] Иерархия realm: dot notation vs slash notation?
- [ ] Factory models: API для множественных экземпляров?
- [ ] Кросс-realm listeners: синтаксис `_$realm:model_property_up`?
- [ ] Автоматическая генерация типов vs ручное объявление?

---

**Версия:** v5.0.0-alpha
**Последнее обновление:** 2025-01-24
